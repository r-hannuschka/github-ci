name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:

    - name: Checkout
      uses: actions/checkout@v1

    - name: Wait Action github/actions directory
      uses: ./.github/actions/wait
      with:
        milliseconds: 5000
