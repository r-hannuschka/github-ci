name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:

    - name: Checkout
      uses: actions/checkout@v1

    - name: Wait Action
      uses: ./.github/actions/wait
      with:
        milliseconds: 1000
